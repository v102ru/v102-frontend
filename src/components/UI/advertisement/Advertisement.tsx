import { FC } from "react";
import "./Advertisement.scss";

const Advertisement: FC = ({}) => {
  return (
    <div className={"advertisement"}>
      <span className={"advertisement__text"}>Реклама</span>
    </div>
  );
};

export default Advertisement;
